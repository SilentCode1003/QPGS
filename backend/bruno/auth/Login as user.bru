meta {
  name: Login as user
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "alice",
    "password": "inthewonderland"
  }
}
